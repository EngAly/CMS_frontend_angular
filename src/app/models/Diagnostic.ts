/**
 * data type for selected diseases or medical... etc that store
 *  Disease / medical etc name and if it found in patient or not
 */
export class Diagnostic {
    isDone: boolean;
    name: string;
}